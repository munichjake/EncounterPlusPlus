# E-Mail-Konfiguration für OTP-Login
EMAIL_USER=deine-email@gmail.com
EMAIL_PASSWORD=dein-app-passwort
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587

# Erlaubte E-Mail-Adressen (kommagetrennt)
ALLOWED_EMAILS=user1@example.com,user2@example.com,admin@example.com

# Encryption key für sensible Daten (AES-256)
# Generiere einen Key mit: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# WICHTIG: Einmal generiert, NIEMALS ändern, sonst können verschlüsselte Daten nicht mehr entschlüsselt werden!
ENCRYPTION_KEY=

# CORS - Erlaubte Origins (kommagetrennt)
# Für Single-Domain-Setup: https://yourdomain.com
# Für Subdomain-Setup: https://app.yourdomain.com,https://api.yourdomain.com
# Development: http://localhost:5173,http://localhost:4173
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4173

# Server Port
PORT=4000

# Node Environment (production oder development)
NODE_ENV=development
